<?xml version="1.0" encoding="utf-8"?>
<!--
  The one directory the camera app may write into: photos of a medical document, taken from the
  Import page, under the app's own cache. Nothing else is offered through the provider.
-->
<paths>
    <cache-path name="camera" path="camera/" />
</paths>
