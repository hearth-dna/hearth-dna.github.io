# The version of record for the Android app. `versionName` is what users see; `versionCode` is the
# integer Play orders builds by and permanently refuses to reuse. Bump both in the same commit.
VERSION_NAME=0.2.0
VERSION_CODE=2
