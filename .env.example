# Copy to .env and fill in. .env is gitignored — never commit it.

# Port the local backend listens on. `make dev` proxies /api from the Vite dev server to it.
PORT=8080

# Optional. An operator LLM key for the /v1/ask helper. Leave empty: the endpoint then only
# accepts requests that carry the user's own key (BYOK) in the X-Hearth-Byok header. The app never
# needs this endpoint — tiers 0–2 of Ask are fully local (docs/design.md §6.3).
LLM_API_KEY=

# Optional shared secret an edge proxy sets on requests to a self-hosted backend. Empty = check
# disabled, which is the case for `make dev` and for the GitHub Pages deployment, which has no
# backend at all (docs/decisions/0005-github-pages-hosting.md).
EDGE_SHARED_SECRET=

# Comma-separated origins allowed by CORS. Local dev needs the Vite origin.
WEB_ORIGINS=http://localhost:5180
